# @package _global_

defaults:
  - _self_
  - data: mocha_timit # choose datamodule with `test_dataloader()` for evaluation
  - model: pitch
  - logger: tensorboard
  - trainer: default
  - paths: default
  - extras: default
  - hydra: default
  - callbacks: default

  - debug: null

task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: ???


